<%
  const productMap = {};
  if (typeof loanProducts !== 'undefined' && loanProducts) {
    loanProducts.forEach(p => {
      productMap[p.key] = p.label;
      if (p.children) p.children.forEach(c => { productMap[c.key] = p.label; });
    });
  }
  function getProductName(key) { return key ? (productMap[key] || key) : '-'; }
%>
<% if (typeof success !== 'undefined' && success && success.length > 0) { %>
  <div class="bg-green-50 dark:bg-green-900/20 text-green-700 dark:text-green-300 text-sm px-4 py-3 rounded-xl mb-6">
    <%= success %>
  </div>
<% } %>

<!-- Stats Cards -->
<div class="grid grid-cols-2 sm:grid-cols-4 gap-4 mb-8">
  <div class="bg-white dark:bg-gray-900 rounded-2xl shadow-sm border border-gray-200 dark:border-gray-800 p-5">
    <div class="flex items-center gap-3 mb-3">
      <div class="w-9 h-9 rounded-xl bg-green-100 dark:bg-green-900/30 flex items-center justify-center">
        <svg class="w-5 h-5 text-green-600 dark:text-green-400" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z"/></svg>
      </div>
    </div>
    <p class="text-2xl font-bold text-gray-900 dark:text-gray-100"><%= stats.today %></p>
    <p class="text-xs text-gray-500 dark:text-gray-400 mt-1">Hari Ini</p>
  </div>

  <div class="bg-white dark:bg-gray-900 rounded-2xl shadow-sm border border-gray-200 dark:border-gray-800 p-5">
    <div class="flex items-center gap-3 mb-3">
      <div class="w-9 h-9 rounded-xl bg-blue-100 dark:bg-blue-900/30 flex items-center justify-center">
        <svg class="w-5 h-5 text-blue-600 dark:text-blue-400" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 7V3m8 4V3m-9 8h10M5 21h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v12a2 2 0 002 2z"/></svg>
      </div>
    </div>
    <p class="text-2xl font-bold text-gray-900 dark:text-gray-100"><%= stats.thisWeek %></p>
    <p class="text-xs text-gray-500 dark:text-gray-400 mt-1">Minggu Ini</p>
  </div>

  <div class="bg-white dark:bg-gray-900 rounded-2xl shadow-sm border border-gray-200 dark:border-gray-800 p-5">
    <div class="flex items-center gap-3 mb-3">
      <div class="w-9 h-9 rounded-xl bg-amber-100 dark:bg-amber-900/30 flex items-center justify-center">
        <svg class="w-5 h-5 text-amber-600 dark:text-amber-400" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 19v-6a2 2 0 00-2-2H5a2 2 0 00-2 2v6a2 2 0 002 2h2a2 2 0 002-2zm0 0V9a2 2 0 012-2h2a2 2 0 012 2v10m-6 0a2 2 0 002 2h2a2 2 0 002-2m0 0V5a2 2 0 012-2h2a2 2 0 012 2v14a2 2 0 01-2 2h-2a2 2 0 01-2-2z"/></svg>
      </div>
    </div>
    <p class="text-2xl font-bold text-gray-900 dark:text-gray-100"><%= stats.thisMonth %></p>
    <p class="text-xs text-gray-500 dark:text-gray-400 mt-1">Bulan Ini</p>
  </div>

  <div class="bg-white dark:bg-gray-900 rounded-2xl shadow-sm border border-gray-200 dark:border-gray-800 p-5">
    <div class="flex items-center gap-3 mb-3">
      <div class="w-9 h-9 rounded-xl bg-primary-100 dark:bg-primary-900/30 flex items-center justify-center">
        <svg class="w-5 h-5 text-primary-600 dark:text-primary-400" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z"/></svg>
      </div>
    </div>
    <p class="text-2xl font-bold text-gray-900 dark:text-gray-100"><%= stats.total %></p>
    <p class="text-xs text-gray-500 dark:text-gray-400 mt-1">Total Cases</p>
  </div>
</div>

<!-- Agent Summary (admin/superadmin) -->
<% if (user.role === 'superadmin' || user.role === 'admin') { %>
<div class="grid grid-cols-1 sm:grid-cols-3 gap-4 mb-8">
  <div class="bg-white dark:bg-gray-900 rounded-2xl shadow-sm border border-gray-200 dark:border-gray-800 p-5 flex items-center gap-4">
    <div class="w-10 h-10 rounded-xl bg-indigo-100 dark:bg-indigo-900/30 flex items-center justify-center">
      <svg class="w-5 h-5 text-indigo-600 dark:text-indigo-400" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 20h5v-2a3 3 0 00-5.356-1.857M17 20H7m10 0v-2c0-.656-.126-1.283-.356-1.857M7 20H2v-2a3 3 0 015.356-1.857M7 20v-2c0-.656.126-1.283.356-1.857m0 0a5.002 5.002 0 019.288 0M15 7a3 3 0 11-6 0 3 3 0 016 0z"/></svg>
    </div>
    <div>
      <p class="text-xl font-bold text-gray-900 dark:text-gray-100"><%= totalAgents %></p>
      <p class="text-xs text-gray-500 dark:text-gray-400">Total Agent</p>
    </div>
  </div>
  <div class="bg-white dark:bg-gray-900 rounded-2xl shadow-sm border border-gray-200 dark:border-gray-800 p-5 flex items-center gap-4">
    <div class="w-10 h-10 rounded-xl bg-blue-100 dark:bg-blue-900/30 flex items-center justify-center">
      <svg class="w-5 h-5 text-blue-600 dark:text-blue-400" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z"/></svg>
    </div>
    <div>
      <p class="text-xl font-bold text-gray-900 dark:text-gray-100"><%= totalMaster %></p>
      <p class="text-xs text-gray-500 dark:text-gray-400">Master Agent</p>
    </div>
  </div>
  <div class="bg-white dark:bg-gray-900 rounded-2xl shadow-sm border border-gray-200 dark:border-gray-800 p-5 flex items-center gap-4">
    <div class="w-10 h-10 rounded-xl bg-gray-100 dark:bg-gray-800 flex items-center justify-center">
      <svg class="w-5 h-5 text-gray-600 dark:text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z"/></svg>
    </div>
    <div>
      <p class="text-xl font-bold text-gray-900 dark:text-gray-100"><%= totalSub %></p>
      <p class="text-xs text-gray-500 dark:text-gray-400">Sub Agent</p>
    </div>
  </div>
</div>
<% } %>

<% if (typeof referralCode !== 'undefined' && referralCode && user.role !== 'superadmin' && user.role !== 'admin') { %>
<!-- Referral Links -->
<div class="bg-white dark:bg-gray-900 rounded-2xl shadow-sm border border-gray-200 dark:border-gray-800 p-6 mb-8">
  <div class="mb-4">
    <p class="text-sm font-medium text-gray-700 dark:text-gray-300 mb-1">Your Public Submission Links</p>
    <p class="text-xs text-gray-400 mb-3">Share these links with customers. Each product has its own unique URL.</p>
  </div>

  <!-- General link -->
  <div class="mb-4">
    <label class="text-xs font-medium text-gray-500 dark:text-gray-400 mb-1 block">General (all products)</label>
    <div class="flex items-center gap-2">
      <code class="flex-1 min-w-0 text-xs bg-gray-100 dark:bg-gray-800 px-3 py-2 rounded-lg font-mono truncate" id="refLinkGeneral"><%= baseUrl %>/submit?ref=<%= referralCode %></code>
      <button onclick="copyToClipboard('refLinkGeneral', this)" class="shrink-0 px-3 py-2 bg-primary-600 hover:bg-primary-700 text-white rounded-lg text-xs font-medium transition-colors">Copy</button>
    </div>
  </div>

  <!-- Product-specific links -->
  <% if (typeof enabledProducts !== 'undefined' && enabledProducts.length > 0) { %>
  <div class="space-y-2">
    <label class="text-xs font-medium text-gray-500 dark:text-gray-400 block">Product-specific links</label>
    <% enabledProducts.forEach((p, i) => { %>
      <div class="flex items-center gap-2">
        <span class="text-xs text-gray-500 dark:text-gray-400 w-32 sm:w-40 shrink-0 truncate" title="<%= p.label %>"><%= p.label %></span>
        <code class="flex-1 min-w-0 text-xs bg-gray-100 dark:bg-gray-800 px-3 py-2 rounded-lg font-mono truncate" id="refLink_<%= i %>"><%= baseUrl %>/submit?ref=<%= referralCode %>&product=<%= p.key %></code>
        <button onclick="copyToClipboard('refLink_<%= i %>', this)" class="shrink-0 px-3 py-2 bg-primary-600 hover:bg-primary-700 text-white rounded-lg text-xs font-medium transition-colors">Copy</button>
      </div>
    <% }) %>
  </div>
  <% } %>

  <div class="mt-4 pt-3 border-t border-gray-100 dark:border-gray-800 flex items-center gap-2">
    <p class="text-xs text-gray-400">Referral Code:</p>
    <p class="text-sm font-bold font-mono text-primary-600 dark:text-primary-400"><%= referralCode %></p>
  </div>
</div>
<script>
function copyToClipboard(id, btn) {
  const text = document.getElementById(id).textContent.trim();
  const onSuccess = () => {
    const orig = btn.textContent;
    btn.textContent = 'Copied!';
    setTimeout(() => { btn.textContent = orig; }, 2000);
  };
  if (navigator.clipboard && window.isSecureContext) {
    navigator.clipboard.writeText(text).then(onSuccess).catch(() => fallbackCopy(text, onSuccess));
  } else {
    fallbackCopy(text, onSuccess);
  }
}
function fallbackCopy(text, cb) {
  const ta = document.createElement('textarea');
  ta.value = text;
  ta.style.position = 'fixed';
  ta.style.opacity = '0';
  document.body.appendChild(ta);
  ta.select();
  document.execCommand('copy');
  document.body.removeChild(ta);
  cb();
}
</script>
<% } %>

<!-- Recent Cases + Agent Performance -->
<div class="grid grid-cols-1 lg:grid-cols-3 gap-6">
  <!-- Recent Cases (2/3 width) -->
  <div class="lg:col-span-2 bg-white dark:bg-gray-900 rounded-2xl shadow-sm border border-gray-200 dark:border-gray-800">
    <div class="px-4 sm:px-6 py-4 border-b border-gray-200 dark:border-gray-800 flex items-center justify-between">
      <h3 class="text-base font-semibold">
        <% if (user.role === 'superadmin' || user.role === 'admin') { %>
          Unclaimed Cases
          <% if (recentCases.length > 0) { %>
            <span class="ml-2 inline-flex items-center justify-center px-2 py-0.5 rounded-full text-xs font-bold bg-red-100 dark:bg-red-900/30 text-red-600 dark:text-red-400"><%= recentCases.length %></span>
          <% } %>
        <% } else { %>
          Latest Cases
        <% } %>
      </h3>
      <a href="/dashboard/cases" class="text-xs text-primary-600 dark:text-primary-400 hover:underline font-medium">View All →</a>
    </div>
    <div class="overflow-x-auto">
      <table class="w-full text-sm">
        <thead>
          <tr class="border-b border-gray-200 dark:border-gray-800">
            <th class="text-left px-3 sm:px-6 py-3 text-gray-500 dark:text-gray-400 font-medium">Customer</th>
            <th class="text-left px-3 sm:px-6 py-3 text-gray-500 dark:text-gray-400 font-medium hidden sm:table-cell">Product</th>
            <th class="text-left px-3 sm:px-6 py-3 text-gray-500 dark:text-gray-400 font-medium hidden sm:table-cell">Master Agent</th>
            <th class="text-left px-3 sm:px-6 py-3 text-gray-500 dark:text-gray-400 font-medium hidden sm:table-cell">Sub Agent</th>
            <th class="text-left px-3 sm:px-6 py-3 text-gray-500 dark:text-gray-400 font-medium hidden md:table-cell">Tarikh</th>
            <th class="text-center px-3 sm:px-6 py-3 text-gray-500 dark:text-gray-400 font-medium">Action</th>
          </tr>
        </thead>
        <tbody id="recentCasesBody">
          <% if (recentCases.length === 0) { %>
            <tr><td colspan="6" class="px-3 sm:px-6 py-8 text-center text-gray-400">No cases</td></tr>
          <% } %>
          <% recentCases.forEach(s => { %>
            <tr class="recent-row border-b border-gray-100 dark:border-gray-800 hover:bg-gray-50 dark:hover:bg-gray-800/50 transition-colors">
              <td class="px-3 sm:px-6 py-3">
                <div class="font-medium"><%= s.applicant_name %></div>
                <div class="text-xs text-gray-400 sm:hidden"><%= s.applicant_ic !== '-' ? s.applicant_ic : '' %></div>
              </td>
              <td class="px-3 sm:px-6 py-3 text-gray-500 dark:text-gray-400 text-xs hidden sm:table-cell"><%= getProductName(s.product_key) %></td>
              <td class="px-3 sm:px-6 py-3 text-gray-500 dark:text-gray-400 hidden sm:table-cell"><%= s.masteragent_name %></td>
              <td class="px-3 sm:px-6 py-3 text-gray-500 dark:text-gray-400 hidden sm:table-cell"><%= s.subagent_name || '-' %></td>
              <td class="px-3 sm:px-6 py-3 text-gray-500 dark:text-gray-400 text-xs hidden md:table-cell"><%= new Date(s.created_at).toLocaleDateString('ms-MY') %></td>
              <td class="px-3 sm:px-6 py-3 text-center">
                <div class="flex items-center justify-center gap-1.5">
                  <a href="/dashboard/cases/<%= s.id %>" class="px-2.5 py-1 bg-primary-100 dark:bg-primary-900/30 text-primary-700 dark:text-primary-300 rounded-lg text-xs font-medium hover:bg-primary-200 dark:hover:bg-primary-900/50 transition-colors">View</a>
                  <% if (user.role === 'superadmin' || user.role === 'admin') { %>
                  <form method="POST" action="/dashboard/cases/<%= s.id %>/take" class="inline">
                    <button type="submit" class="px-2.5 py-1 bg-green-100 dark:bg-green-900/30 text-green-700 dark:text-green-300 rounded-lg text-xs font-medium hover:bg-green-200 dark:hover:bg-green-900/50 transition-colors">Take</button>
                  </form>
                  <% } %>
                </div>
              </td>
            </tr>
          <% }) %>
        </tbody>
      </table>
    </div>
    <%- include('../partials/pagination', { tableId: 'recentCasesBody', perPage: 5, rowClass: 'recent-row' }) %>
  </div>

  <% if (user.role === 'superadmin' || user.role === 'admin') { %>
  <!-- Activity Feed (1/3 width) -->
  <div class="bg-white dark:bg-gray-900 rounded-2xl shadow-sm border border-gray-200 dark:border-gray-800">
    <div class="px-4 sm:px-6 py-4 border-b border-gray-200 dark:border-gray-800 flex items-center justify-between">
      <h3 class="text-base font-semibold">🔔 Aktiviti</h3>
      <div class="flex items-center gap-2">
        <span class="text-[10px] text-gray-400">🟢 Live</span>
        <a href="/dashboard/notifications" class="text-xs text-primary-600 dark:text-primary-400 hover:underline font-medium">All →</a>
      </div>
    </div>
    <div id="activityFeed" class="divide-y divide-gray-100 dark:divide-gray-800 overflow-y-auto" style="max-height: 400px;">
      <div class="px-4 py-6 text-center text-gray-400 text-xs" id="activityEmpty">No recent activity</div>
    </div>
  </div>
  <% } else { %>
  <!-- Agent Performance (1/3 width) - for non-admin -->
  <div class="bg-white dark:bg-gray-900 rounded-2xl shadow-sm border border-gray-200 dark:border-gray-800">
    <div class="px-4 sm:px-6 py-4 border-b border-gray-200 dark:border-gray-800">
      <h3 class="text-base font-semibold"><%= (user.role === 'masteragent') ? 'Sub Agent Performance' : 'Master Agent Performance' %></h3>
    </div>
    <div class="overflow-y-auto" style="max-height: 400px;">
      <% if (performance.length === 0) { %>
        <div class="px-4 sm:px-6 py-8 text-center text-gray-400 text-sm">No agents found</div>
      <% } %>
      <% performance.sort((a, b) => b.total - a.total).forEach((p, i) => { %>
        <div class="flex items-center gap-3 px-4 sm:px-6 py-3 border-b border-gray-100 dark:border-gray-800 last:border-0 hover:bg-gray-50 dark:hover:bg-gray-800/50 transition-colors">
          <div class="w-7 h-7 rounded-full flex items-center justify-center text-xs font-bold
            <% if (i === 0) { %>bg-amber-100 dark:bg-amber-900/30 text-amber-700 dark:text-amber-300
            <% } else if (i === 1) { %>bg-gray-200 dark:bg-gray-700 text-gray-600 dark:text-gray-300
            <% } else if (i === 2) { %>bg-orange-100 dark:bg-orange-900/30 text-orange-700 dark:text-orange-300
            <% } else { %>bg-gray-100 dark:bg-gray-800 text-gray-500 dark:text-gray-400<% } %>"><%= i + 1 %></div>
          <div class="flex-1 min-w-0">
            <p class="text-sm font-medium truncate"><%= p.name %></p>
          </div>
          <div class="text-right">
            <p class="text-sm font-bold"><%= p.total %></p>
            <p class="text-xs text-gray-400">kes</p>
          </div>
        </div>
      <% }) %>
    </div>
  </div>
  <% } %>
</div>

<% if (user.role === 'superadmin' || user.role === 'admin') { %>
<!-- Agent Performance (full width below) -->
<div class="mt-6 bg-white dark:bg-gray-900 rounded-2xl shadow-sm border border-gray-200 dark:border-gray-800">
  <div class="px-4 sm:px-6 py-4 border-b border-gray-200 dark:border-gray-800">
    <h3 class="text-base font-semibold"><%= (user.role === 'masteragent') ? 'Sub Agent Performance' : 'Master Agent Performance' %></h3>
  </div>
  <div class="overflow-y-auto" style="max-height: 400px;">
    <% if (performance.length === 0) { %>
      <div class="px-4 sm:px-6 py-8 text-center text-gray-400 text-sm">No agents found</div>
    <% } %>
    <% performance.sort((a, b) => b.total - a.total).forEach((p, i) => { %>
      <div class="flex items-center gap-3 px-4 sm:px-6 py-3 border-b border-gray-100 dark:border-gray-800 last:border-0 hover:bg-gray-50 dark:hover:bg-gray-800/50 transition-colors">
        <div class="w-7 h-7 rounded-full flex items-center justify-center text-xs font-bold
          <% if (i === 0) { %>bg-amber-100 dark:bg-amber-900/30 text-amber-700 dark:text-amber-300
          <% } else if (i === 1) { %>bg-gray-200 dark:bg-gray-700 text-gray-600 dark:text-gray-300
          <% } else if (i === 2) { %>bg-orange-100 dark:bg-orange-900/30 text-orange-700 dark:text-orange-300
          <% } else { %>bg-gray-100 dark:bg-gray-800 text-gray-500 dark:text-gray-400<% } %>"><%= i + 1 %></div>
        <div class="flex-1 min-w-0">
          <p class="text-sm font-medium truncate"><%= p.name %></p>
        </div>
        <div class="text-right">
          <p class="text-sm font-bold"><%= p.total %></p>
          <p class="text-xs text-gray-400">kes</p>
        </div>
      </div>
    <% }) %>
  </div>
</div>

<!-- Toast container -->
<div id="toastContainer" class="fixed top-4 right-4 z-50 flex flex-col gap-2" style="pointer-events:none;"></div>

<style>
  @keyframes highlightRow { from { background: rgba(14,165,233,0.15); } to { background: transparent; } }
  @keyframes toastIn { from { opacity:0; transform:translateX(100%); } to { opacity:1; transform:translateX(0); } }
  @keyframes toastOut { from { opacity:1; transform:translateX(0); } to { opacity:0; transform:translateX(100%); } }
  .toast-item { pointer-events:auto; animation: toastIn 0.3s ease-out; }
  .toast-item.removing { animation: toastOut 0.3s ease-in forwards; }
</style>
<script>
(function() {
  const proto = location.protocol === 'https:' ? 'wss:' : 'ws:';
  let ws;

  function connect() {
    ws = new WebSocket(proto + '//' + location.host + '/ws/notifications');
    ws.onmessage = function(event) {
      try {
        const data = JSON.parse(event.data);
        if (data.type === 'NEW_CASE') {
          addCaseRow(data);
          updateBadge(1);
          addActivityItem('🆕', data.applicantName, 'Permohonan baru', data.caseId);
        } else if (data.type === 'CASE_TAKEN') {
          removeCaseRow(data.caseId);
          updateBadge(-1);
          addActivityItem('✅', data.takerName || 'Admin', 'Case taken', data.caseId);
        } else if (data.type === 'CASE_MODIFIED') {
          showToast(data);
          addActivityItem('📝', data.applicantName, data.detail || 'Documents updated', data.caseId);
        }
      } catch(e) {}
    };
    ws.onclose = function() { setTimeout(connect, 5000); };
    ws.onerror = function() { ws.close(); };
  }

  function addCaseRow(data) {
    const tbody = document.getElementById('recentCasesBody');
    if (!tbody) return;
    const emptyRow = tbody.querySelector('td[colspan]');
    if (emptyRow) emptyRow.closest('tr').remove();
    const tr = document.createElement('tr');
    tr.className = 'recent-row border-b border-gray-100 dark:border-gray-800 hover:bg-gray-50 dark:hover:bg-gray-800/50 transition-colors';
    tr.dataset.caseId = data.caseId;
    tr.style.animation = 'highlightRow 3s ease-out';
    tr.innerHTML = `
      <td class="px-3 sm:px-6 py-3"><div class="font-medium">${data.applicantName}</div></td>
      <td class="px-3 sm:px-6 py-3 text-gray-500 dark:text-gray-400 text-xs hidden sm:table-cell">-</td>
      <td class="px-3 sm:px-6 py-3 text-gray-500 dark:text-gray-400 hidden sm:table-cell">${data.agentName}</td>
      <td class="px-3 sm:px-6 py-3 text-gray-500 dark:text-gray-400 hidden sm:table-cell">-</td>
      <td class="px-3 sm:px-6 py-3 text-gray-500 dark:text-gray-400 text-xs hidden md:table-cell">${data.date}</td>
      <td class="px-3 sm:px-6 py-3 text-center">
        <a href="/dashboard/cases/${data.caseId}" class="px-2.5 py-1 bg-primary-100 dark:bg-primary-900/30 text-primary-700 dark:text-primary-300 rounded-lg text-xs font-medium hover:bg-primary-200 dark:hover:bg-primary-900/50 transition-colors">View</a>
      </td>`;
    tbody.insertBefore(tr, tbody.firstChild);
  }

  function removeCaseRow(caseId) {
    const tbody = document.getElementById('recentCasesBody');
    if (!tbody) return;
    let row = tbody.querySelector('tr[data-case-id="' + caseId + '"]');
    if (!row) {
      const link = tbody.querySelector('a[href="/dashboard/cases/' + caseId + '"]');
      if (link) row = link.closest('tr');
    }
    if (row) {
      row.style.transition = 'opacity 0.5s';
      row.style.opacity = '0';
      setTimeout(() => {
        row.remove();
        if (!tbody.querySelector('tr')) {
          const tr = document.createElement('tr');
          tr.innerHTML = '<td colspan="6" class="px-3 sm:px-6 py-8 text-center text-gray-400">No cases</td>';
          tbody.appendChild(tr);
        }
      }, 500);
    }
  }

  function updateBadge(delta) {
    const badge = document.querySelector('#recentCasesBody')?.closest('.bg-white, .dark\\:bg-gray-900')?.querySelector('h3 span');
    if (badge) {
      const newCount = Math.max(0, parseInt(badge.textContent || '0') + delta);
      if (newCount > 0) { badge.textContent = newCount; } else { badge.remove(); }
    }
  }

  function showToast(data) {
    const container = document.getElementById('toastContainer');
    if (!container) return;
    const toast = document.createElement('div');
    toast.className = 'toast-item flex items-center gap-3 px-4 py-3 rounded-xl shadow-lg border border-orange-200 dark:border-orange-800 bg-orange-50 dark:bg-orange-900/90 max-w-sm cursor-pointer';
    toast.innerHTML = `
      <span class="text-lg">📝</span>
      <div class="flex-1 min-w-0">
        <p class="text-sm font-semibold text-orange-800 dark:text-orange-200 truncate">${data.applicantName}</p>
        <p class="text-[11px] text-orange-600 dark:text-orange-400">${data.detail || 'Documents updated'}</p>
      </div>
      <span class="text-xs text-orange-500 dark:text-orange-300 font-medium shrink-0">View →</span>`;
    toast.onclick = () => { window.location.href = '/dashboard/cases/' + data.caseId; };
    container.appendChild(toast);
    setTimeout(() => { toast.classList.add('removing'); setTimeout(() => toast.remove(), 300); }, 5000);
  }

  function addActivityItem(icon, title, message, caseId) {
    const feed = document.getElementById('activityFeed');
    if (!feed) return;
    const empty = document.getElementById('activityEmpty');
    if (empty) empty.remove();
    const item = document.createElement('a');
    item.href = '/dashboard/cases/' + caseId;
    item.className = 'flex items-center gap-3 px-4 sm:px-6 py-3 hover:bg-gray-50 dark:hover:bg-gray-800/50 transition-colors';
    item.style.animation = 'highlightRow 2s ease-out';
    const now = new Date().toLocaleTimeString('ms-MY', { hour: '2-digit', minute: '2-digit' });
    item.innerHTML = `
      <span class="text-lg shrink-0">${icon}</span>
      <div class="flex-1 min-w-0">
        <p class="text-sm font-medium truncate">${title}</p>
        <p class="text-[11px] text-gray-500 dark:text-gray-400">${message}</p>
      </div>
      <span class="text-[10px] text-gray-400 shrink-0">${now}</span>`;
    feed.insertBefore(item, feed.firstChild);
    // Cap at 20
    while (feed.children.length > 20) feed.removeChild(feed.lastChild);
  }

  connect();
})();
</script>
<% } %>
<%- include('../partials/pagination-script') %>
